DONE:
--Can create clients.
--Can create contacts that are linked to specific client.
--Superuser can see a list of user-specific reviews/clients.
--Added buttons to main page (my reviews/my clients).
--Can now give user access to review (one by one, no bulk).
--Added link to see "Parue" and "Non-parue" reviews
--Changed "Nom du client" to "Nom de l'entreprise" in myClients.php
--Now INSERTing phone number per client/company in clientCreate.php
--Now client and contact names are UPPERCASE
--Added "Paru" column and filter link myReviews.php
--Removed "+" button to create client (myClients and userClients)
--Renamed and replaced searchReview.php to reviewSearch.php, replaced searchClientOrders.php with clientReviews.php
--Created reviewClients.php and added "+" button only in reviewClients.php
--Created clientReviews.php
--Added sort by published link on userReviews.php and "Parue" column
--Removed useless "Interagir" columns
--Moved contacts link to "Contacts" column with icon in myClients.php and userClients.php and reviewClients.php
--Now there is a link to clientReviews.php and reviewClients.php in myReviews.php, userReviews.php, myClients.php, userClients.php
--Added link to comments $commentsLink in reviewClients.php and reviewClients.php

URGENT:
--edit index.php
--make phone number required on client creation
--Change SQL requests in commentList.php and commentAdd.php, add clientReviews.php and reviewClients.php links
--add column to which review is linked specific client
--search bars myClients.php myReviews.php
--change clientList to myClients.php where needed

TODO:
--Edit/delete buttons for clients
--Link/Unlik clients to reviews somehow...... (maybe using webcommercial_client_revue table ?)
--Image link "+" that links to all reviews linked to client
--Be able to import CSV file, 1st line columns names, next line columns data:
----CSV line contains information about the CLIENT and the CONTACT
----If SIRET already exists, it means it's another contact. SELECT clientId linked to SIRET and create the contact.
--User1 can see user2's clients if granted, but one-way, user2 cannot see user1's clients if not granted. -> or <->
--Add edit/delete image links everywhere (client/contact)

OPTIONAL:
++Inserter to read table name, DESCRIBE table, get columns names and properties, and create form to INSERT DELETE UPDATE automatically, myPhpAdmin
